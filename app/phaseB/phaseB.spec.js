'use strict';

describe('myApp.phaseB module', function() {

  beforeEach(module('myApp.phaseB'));

  describe('phaseB controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var PhaseB2Ctrl = $controller('PhaseBCtrl');
      expect(PhaseBCtrl).toBeDefined();
    }));

  });
});