'use strict';

describe('myApp.phaseC2 module', function() {

  beforeEach(module('myApp.phaseC2'));

  describe('phaseC2 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var PhaseC2Ctrl = $controller('PhaseC2Ctrl');
      expect(PhaseC2Ctrl).toBeDefined();
    }));

  });
});