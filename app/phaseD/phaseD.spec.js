'use strict';

describe('myApp.phaseD module', function() {

  beforeEach(module('myApp.phaseD'));

  describe('phaseD controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var PhaseDCtrl = $controller('PhaseDCtrl');
      expect(PhaseDCtrl).toBeDefined();
    }));

  });
});