'use strict';

describe('myApp.phaseC1 module', function() {

  beforeEach(module('myApp.phaseC1'));

  describe('phaseC1 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var PhaseC1Ctrl = $controller('PhaseC1Ctrl');
      expect(PhaseC1Ctrl).toBeDefined();
    }));

  });
});